Studio Anwar — Logo Quick Start (generated 20250828-105831)

Files:
- studio-anwar_lockup_horizontal_20250828-105831.svg  → Primary horizontal lockup (SVG, scalable, transparent)
- studio-anwar_icon_20250828-105831.svg    → Compact square icon (SVG, scalable, transparent)

How to match your current site colors & font:
1) Open either SVG in a code editor.
2) Replace the CSS variables at the top:
   --brand-primary: #YOUR_PRIMARY_HEX;
   --brand-text:    #YOUR_TEXT_HEX;
   --brand-grid:    rgba(r,g,b,alpha);   // use a subtle low‑alpha tint from your palette
   --brand-font:    YOUR_SITE_FONT_STACK; // e.g. "Söhne", "Inter", system-ui
3) Save — you’re done. Exports to PNG/WebP work perfectly from most vector tools.

Notes:
- The emblem uses a subtle 3×3 grid and modular blocks to suggest layout discipline and craft.
- Corner radius and tracking (letter spacing) are tweakable via --corner and --track.
- Grid lines are intentionally light; set --brand-grid to 'none' to remove them entirely.

License: Yours to use and modify for Studio Anwar.
